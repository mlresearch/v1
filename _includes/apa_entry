{% assign lastone = page.author.last %}{% for person in page.author %}{{ person.family }}, {{ person.given }}{% unless person == lastone %}, {%endunless %}{% endfor %}. ({{ site.published | date: "%Y" }}). {{ page.title }}. <i>{{ page.booktitle }}, in PMLR {{ site.volume }}</i>, {{page.firstpage}}--{{page.lastpage}}
